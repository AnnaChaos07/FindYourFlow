services:
  app:
    build: .
    init: true
    ports:
      - "3000:3000"
    environment:
      DEV_ORIGIN: ${DEV_ORIGIN:-http://localhost:3000}
    volumes:
      - .:/app
      - node_modules:/app/node_modules
      - next_cache:/app/.next

volumes:
  node_modules:
  next_cache:
